# Use the referenced project

## Changes
- Replace the current starter screen with the accessible `sparkle-kit-joy` project contents.
- Preserve its existing pages, interactions, styling, and assets.
- Increase the landing headline weight so both “Build software” and “by simply describing it” render bold.
- Verify the imported app loads successfully and the headline is visibly bold.

## Technical details
- Copy the repository source and configuration into this project without Git metadata or dependency/build folders.
- Update the existing landing headline’s typography class rather than changing its wording or layout.
- Retain the project’s TanStack Start structure and route metadata.
